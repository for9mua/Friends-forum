import { Ref } from 'vue';
/**
 * 用与保存emoji到本地
 * @param recentData 最近使用的数据
 * @param currentEmoji 当前选择的emoji
 */
export declare const saveToLocal: (recentData: Ref<Emoji.JsonData>, currentEmoji: Emoji.EmojiItem) => void;
